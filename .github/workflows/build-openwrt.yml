name: Build OpenWrt

on:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'Enable SSH debug session'
        required: false
        type: boolean
        default: false
      config_url:
        description: 'Optional raw URL of .config (takes priority over repository .config)'
        required: false
        type: string
        default: ''
  repository_dispatch:

env:
  REPO_URL: https://github.com/VIKINGYFY/immortalwrt.git
  REPO_BRANCH: main
  CONFIG_FILE: .config
  FEEDS_CONF: feeds.conf.default
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  CUSTOM_SOURCE_DIR: custom
  UPLOAD_BIN_DIR: false
  TZ: Asia/Shanghai
  # 强制所有使用旧版 Node 的 Actions（如 ssh2actions）直接在 Node 24 环境下运行，屏蔽警告
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5 # 升级到 v5 (原生支持 Node 24)

      - name: Free disk space (Optimize runner)
        # 编译 OpenWrt 非常占用空间，提前移除 Runner 镜像中自带的无用庞大组件，避免编译期由于磁盘打满而失败
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo apt-get clean
          df -h

      - name: 1) Create OpenWrt build environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
            sudo -E apt-get -qq update # 加上 -E 参数，确保 sudo 继承 DEBIAN_FRONTEND 环境隔离设置
            sudo -E apt-get -qq install \
              ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
              bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext \
              gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 \
              libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev \
              libmpfr-dev libncurses-dev libpython3-dev libreadline-dev libssl-dev libtool \
              lrzsz mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf \
              python3 python3-pip python3-ply python3-docutils python3-setuptools rsync \
              scons squashfs-tools subversion swig texinfo node-uglify upx-ucl unzip \
              vim wget xmlto xxd zlib1g-dev zstd
            sudo timedatectl set-timezone "$TZ"

      - name: 2) Download OpenWrt source code
        run: |
          git clone --depth 1 "$REPO_URL" -b "$REPO_BRANCH" openwrt

      - name: 3) Get pre-build resources (feeds)
        run: |
          [ -f "$FEEDS_CONF" ] && cp "$FEEDS_CONF" openwrt/feeds.conf.default
          chmod +x "$DIY_P1_SH"
          cd openwrt
          "$GITHUB_WORKSPACE/$DIY_P1_SH"
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 4) Apply custom source modifications
        run: |
          [ -d files ] && cp -r files openwrt/files
          if[ -d "$CUSTOM_SOURCE_DIR" ]; then
            rsync -a "$CUSTOM_SOURCE_DIR"/ openwrt/
          fi
          # 改用现代化的 inputs 调用方式
          CONFIG_URL="${{ inputs.config_url }}"
          if [ -n "$CONFIG_URL" ]; then
            if ! curl -fsSL "$CONFIG_URL" -o openwrt/.config; then
              echo "config_url download failed, fallback to repository $CONFIG_FILE"
              [ -f "$CONFIG_FILE" ] && cp "$CONFIG_FILE" openwrt/.config
            fi
          elif [ -f "$CONFIG_FILE" ]; then
            cp "$CONFIG_FILE" openwrt/.config
          fi
          chmod +x "$DIY_P2_SH"
          cd openwrt
          mkdir -p dl
          if[ -d "$GITHUB_WORKSPACE/$CUSTOM_SOURCE_DIR/dl" ]; then
            rsync -a "$GITHUB_WORKSPACE/$CUSTOM_SOURCE_DIR/dl"/ dl/
          fi        
          "$GITHUB_WORKSPACE/$DIY_P2_SH"

      - name: Optional SSH debug
        uses: P3TERX/ssh2actions@v1.0.0
        if: (inputs.ssh == true) || contains(github.event.action, 'ssh')
        env:
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}

      - name: 5) Build firmware with existing config
        run: |
          cd openwrt
          make defconfig
          make download -j8
          find dl -size -1024c -exec rm -f {} \;
          make -j"$(nproc)" || make -j1 V=s

      - name: Collect build output info
        id: build_meta
        run: |
          cd openwrt
          grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME || true
          DEVICE_NAME=""
          if[ -s DEVICE_NAME ]; then
            DEVICE_NAME="_$(cat DEVICE_NAME)"
          fi
          FILE_DATE="_$(date +"%Y%m%d%H%M")"
          FIRMWARE_DIR="$(find "$GITHUB_WORKSPACE/openwrt/bin/targets" -mindepth 2 -maxdepth 2 -type d | head -n 1)"
          echo "device_name=$DEVICE_NAME" >> "$GITHUB_OUTPUT"
          echo "file_date=$FILE_DATE" >> "$GITHUB_OUTPUT"
          echo "firmware_dir=$FIRMWARE_DIR" >> "$GITHUB_OUTPUT"

      - name: 6) Upload firmware to GitHub Actions artifacts
        uses: actions/upload-artifact@v5 # 升级到 v5 (原生支持 Node 24)
        with:
          name: OpenWrt_firmware${{ steps.build_meta.outputs.device_name }}${{ steps.build_meta.outputs.file_date }}
          path: ${{ steps.build_meta.outputs.firmware_dir }}
          if-no-files-found: error

      - name: Upload full bin directory (optional)
        if: env.UPLOAD_BIN_DIR == 'true'
        uses: actions/upload-artifact@v5 # 升级到 v5 (原生支持 Node 24)
        with:
          name: OpenWrt_bin${{ steps.build_meta.outputs.device_name }}${{ steps.build_meta.outputs.file_date }}
          path: openwrt/bin
          if-no-files-found: error
